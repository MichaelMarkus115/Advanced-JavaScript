"use strict";

let mile = 1.60934; //1 mile = 1.60934kms

let myNum = Number(prompt("Enter the number you want to convert from Miles to Kilometers: "));

console.log(`The distance of ${myNum} miles is equal to ${myNum*mile} kms`);